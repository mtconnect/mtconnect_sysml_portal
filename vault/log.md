# Change log

> Append-only log of meaningful changes to `SCHEMA.md`, `_meta/taxonomy.md`, and files in `_templates/`. One line per change.

- 2026-01-15 — Vault initialized from `team-vault-template`. SCHEMA, taxonomy, templates, and example folders in place.
- 2026-05-18 — SCHEMA §4: nested-patterns section expanded to cover the three time slices (past/present/active/background) and the in-progress lifecycle. Journal-pattern example gained a nested update-in-place `in-progress/` subfolder demonstrating the active-version case.
- 2026-05-27 — Template example folders (`journal/`, `in-progress/`, `flat-reference/`) removed. Replaced with project-specific domains: `portal/` (journal-driven with nested `in-progress/` and `research/`), `json-schema/`, `generators/`, and `ci-cd/` (all update-in-place). `_meta/taxonomy.md` extended with primary domain tags (`portal`, `json-schema`, `generators`, `ci-cd`) and secondary tags (`xml-schema`, `owl`, `pdf`, `jekyll`, `sysml`, `versioning`, `validation`).
